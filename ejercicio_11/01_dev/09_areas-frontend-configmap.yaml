apiVersion: v1
kind: ConfigMap
metadata:
  name: areas-frontend-configmap
  namespace: dev
data:
  config.json: |
    {
      "API_URL": "http://areas.local/apidashboard",
      "VERSION": "1.0.0"
    }
