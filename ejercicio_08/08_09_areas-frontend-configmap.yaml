apiVersion: v1
kind: ConfigMap
metadata:
  name: areas-frontend-configmap
  namespace: dev
data:
  config.json: |
    {
      "API_URL": "http://areas-backend-service:8080/apidashboard",
      "VERSION": "1.0.0"
    }
